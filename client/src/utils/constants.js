export const reactStarsOptions = {
    edit: false,
    color: 'rgba(20,20,20,0.25)',
    activeColor: '#F4A261',
    size: window.innerWidth <= 900 ? (window.innerWidth < 600 ? 20 : 20) : 22,
    isHalf: true
}
